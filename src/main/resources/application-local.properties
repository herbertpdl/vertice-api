# Local profile: REST auth disabled (see LocalSecurityConfig); gRPC accepts an optional bearer token
# (see GrpcSecurityConfig) for manual testing with tools like Postman or grpcurl.
spring.jpa.show-sql=true

# gRPC reflection lets grpcurl discover services without a .proto file on hand.
spring.grpc.server.reflection.enabled=true
