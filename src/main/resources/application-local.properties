# Local profile: auth disabled (see LocalSecurityConfig) for manual testing with tools like Postman.
spring.jpa.show-sql=true

# gRPC reflection lets grpcurl discover services without a .proto file on hand.
spring.grpc.server.reflection.enabled=true
